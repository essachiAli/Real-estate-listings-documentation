Create a complete, standalone HTML file for the Admin "Review Property" page using HTML + Tailwind CSS 4 (CDN).

Requirements:
- Same admin top navigation.
- Page title: "Review Property – [Title]".
- Full property details display (same fields as edit form, read-only).
- Image gallery grid.
- Submitted by and date.
- Large action buttons at bottom: "Approve" (green), "Reject" (red) – both prominent and full-width on mobile.
- Optional rejection reason textarea (appears when Reject clicked – use Alpine.js or simple show/hide if needed, but static is fine).
- Back button to pending list.
- Responsive and accessible.
- Professional Tailwind design.
- Include Tailwind CDN.

Output only the full HTML code in a ```html block.